__pycache__/
*.py[cod]
.DS_Store
.venv/

# Human approval markers are local state, never committed
APPROVED

# Loop scratch payloads
loop/inbox/

# X API raw responses and follower ids: other people's data, kept on this Mac only
ledger/raw/api/
loop/followers/
